// Mirrors plugin.json (the canonical template generates this via `make apply`;
// vendored statically here so the build is self-contained).
export default {
    id: 'com.mattermost.plugin-message-disappear',
    version: '0.1.0',
};
